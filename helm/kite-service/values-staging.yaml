image:
  repository: "123456789012.dkr.ecr.us-east-1.amazonaws.com/staging/kite-service"
  tag: ""  # set manually via CD workflow promote job before syncing

replicaCount: 2

ingress:
  enabled: true
  hosts:
    - host: kite-staging.example.com
      paths:
        - path: /
          pathType: Prefix

autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 5
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

serviceMonitor:
  enabled: true
  additionalLabels:
    release: kube-prometheus-stack
